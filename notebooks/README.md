# Notebooks

Notebooks are intended for exploratory and reproducible research experiments. Stable functionality should migrate to the Python package under `src/` when it becomes reusable.

Planned notebook sequence will be created incrementally as the research progresses; empty speculative notebooks are intentionally avoided during M0.
